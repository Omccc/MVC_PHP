<h1 class="mb-3 text-center">Admin Add Picture</h1>
<form method="POST">
<div class="mb-3">
  <label for="title" class="form-label">Title</label>
  <input type="text" class="form-control" name="title" id="title" placeholder="Title">
</div>
<div class="mb-3">
  <label for="description" class="form-label">Description</label>
  <input type="text" class="form-control" name="description" id="description" placeholder="Description">
</div>
<div class="mb-3">
  <label for="src" class="form-label">Source</label>
  <input type="text" class="form-control" name="src" id="src" placeholder="URL Source">
</div>
<div class="mb-3">
  <label for="author" class="form-label">Author</label>
  <input type="text" class="form-control" name="author" id="author" placeholder="Author">
</div>
<div class="mb-3">
  <input type="submit" class="btn btn-primary" name="submit" value="Add Picture">
</div>
</form>


































<!-- 
Explications de ce code 

  Ce code génère un formulaire pour ajouter une nouvelle image à la liste. Voici une explication de chaque partie du code :

  1. **`<h1 class="mb-3 text-center">Admin Add Picture</h1>`** :
    - Cette ligne affiche un titre "Admin Add Picture" centré horizontalement en haut de la page, indiquant que cette page est destinée à l'ajout d'une nouvelle image.

  2. **`<form method="POST">`** :
    - Cette balise `<form>` démarre un formulaire HTML qui permettra à l'utilisateur d'ajouter une nouvelle image. La méthode "POST" indique que les données du formulaire seront envoyées au serveur lors de la soumission du formulaire.

  3. **Champs de formulaire** :
    - Il y a plusieurs champs de formulaire, chacun ayant une étiquette (balise `<label>`) et un champ de saisie (balise `<input>`). Voici une explication pour chaque champ :
      - **`<label for="title" class="form-label">Title</label>`** : Un champ de saisie pour le titre de l'image.
      - **`<label for="description" class="form-label">Description</label>`** : Un champ de saisie pour la description de l'image.
      - **`<label for="src" class="form-label">Source</label>`** : Un champ de saisie pour l'URL source de l'image.
      - **`<label for="author" class="form-label">Author</label>`** : Un champ de saisie pour le nom de l'auteur de l'image.

  4. **Bouton "Add Picture"** :
    - **`<input type="submit" class="btn btn-primary" name="submit" value="Add Picture">`** : Ce bouton "Submit" permet à l'utilisateur de soumettre le formulaire. Il est stylisé comme un bouton Bootstrap avec la classe "btn btn-primary". Lorsque l'utilisateur clique sur ce bouton, les données du formulaire seront envoyées au serveur.

  En résumé, ce code génère un formulaire permettant d'ajouter une nouvelle image à la liste. L'utilisateur peut saisir le titre, la description, l'URL source et le nom de l'auteur de l'image, puis soumettre le formulaire pour ajouter l'image à la base de données ou au système sous-jacent. -->