<h1 class="text-center">Admin List</h1>
<a href="?page=adminadd" class="btn btn-primary m-1">Add Picture</a>
<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Source</th>
      <th scope="col">Title</th>
      <th scope="col">Description</th>
      <th scope="col">Author</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
  <?php foreach ($pictures as $pic) : ?>
    <tr data-aos="fade-up">
      <th scope="row"><?= $pic['id'] ?></th>
      <td><img width="80" src="<?= $pic['src'] ?>" alt="<?= $pic['title'] ?>"></td>
      <td><?= $pic['title'] ?></td>
      <td><?= $pic['description'] ?></td>
      <td><?= $pic['author'] ?></td>
      <td>
        <a href="?page=admin_update&id=<?=$pic['id']?>" class="btn btn-warning m-1">Update</a>
        <a href="?page=admin_delete&id=<?=$pic['id']?>" class="btn btn-danger m-1">Delete?</a>
      </td>
    </tr>
  <?php endforeach ?>
  </tbody>
  </table>
  <a href="?page=adminadd" class="btn btn-primary m-1">Add Picture</a>

  <script>

const deleteButtons = document.querySelectorAll('.btn-danger');

for (const button of deleteButtons) {
  button.addEventListener('click', () => {
    const confirmDelete = confirm('Are you sure you want to delete this?');

    if (confirmDelete) {
      
    }
  });
}


  </script>












<!-- Explications de ce code 


  Ce code génère une page d'administration "Admin List" qui affiche une liste de données, probablement des images, dans un tableau. Voici une explication de chaque partie du code :

  1. **`<h1 class="text-center">Admin List</h1>`** :
    - Cette ligne affiche un titre "Admin List" centré horizontalement en haut de la page.

  2. **`<a href="?page=adminadd" class="btn btn-primary m-1">Add Picture</a>`** :
    - C'est un lien "Add Picture" qui dirige vers une page d'ajout d'image. Il est stylisé comme un bouton Bootstrap avec les classes "btn btn-primary" et "m-1".

  3. **`<table class="table table-striped">`** :
    - C'est le début d'un tableau HTML avec des classes Bootstrap pour le formatage. La classe "table" est utilisée pour styliser le tableau, et "table-striped" ajoute des rayures à chaque ligne.

  4. **`<thead>`** :
    - Cette balise contient l'en-tête du tableau.

  5. **`<tr>`** :
    - Cette balise `<tr>` définit une ligne d'en-tête.

  6. **`<th scope="col">#</th>`** :
    - Chaque balise `<th>` représente une cellule d'en-tête de colonne. La colonne "#"" est probablement utilisée pour numéroter les éléments.

  7. **`<th scope="col">Source</th>`** :
    - Cette cellule d'en-tête indique la colonne "Source" pour la source de l'image.

  8. **`<th scope="col">Title</th>`** :
    - Cette cellule d'en-tête indique la colonne "Title" pour le titre de l'image.

  9. **`<th scope="col">Description</th>`** :
    - Cette cellule d'en-tête indique la colonne "Description" pour la description de l'image.

  10. **`<th scope="col">Author</th>`** :
      - Cette cellule d'en-tête indique la colonne "Author" pour l'auteur de l'image.

  11. **`<th scope="col">Actions</th>`** :
      - Cette cellule d'en-tête indique la colonne "Actions" pour les actions à effectuer sur chaque image.

  12. **`<tbody>`** :
      - Cette balise contient le corps du tableau, où les données seront affichées.

  13. **`< ?php foreach ($pictures as $pic) : ?>`** :
      - C'est le début d'une boucle `foreach` en PHP. Elle parcourt le tableau `$pictures`, qui contient les informations des images.

  14. **`<tr>`** :
      - Cette balise `<tr>` définit une nouvelle ligne dans le tableau pour chaque image.

  15. **`<th scope="row">< ?= $pic['id'] ?></th>`** :
      - Cette cellule `<th>` affiche l'ID de l'image, probablement pour identifier chaque image de manière unique.

  16. **`<td><img width="80" src="< ?= $pic['src'] ?>" alt="< ?= $pic['title'] ?>"></td>`** :
      - Cette cellule `<td>` affiche une image réduite (80 pixels de large) extraite de la source "src". L'attribut "alt" contient le titre de l'image.

  17. **`<td>< ?= $pic['title'] ?></td>`** :
      - Cette cellule `<td>` affiche le titre de l'image.

  18. **`<td>< ?= $pic['description'] ?></td>`** :
      - Cette cellule `<td>` affiche la description de l'image.

  19. **`<td>< ?= $pic['author'] ?></td>`** :
      - Cette cellule `<td>` affiche l'auteur de l'image.

  20. **Les boutons "Update" et "Delete"** :
      - Chaque ligne contient des boutons "Update" et "Delete" qui permettent à l'administrateur de mettre à jour ou de supprimer chaque image. Les boutons sont stylisés comme des boutons Bootstrap avec des classes "btn btn-warning" et "btn btn-danger".

  21. **Le bloc de script JavaScript** :
      - Ce script ajoute une fonctionnalité de confirmation de suppression. Lorsque l'utilisateur clique sur un bouton "Delete", une boîte de dialogue de confirmation s'affiche. Si l'utilisateur confirme la suppression, l'action de suppression est exécutée.

  En résumé, cette page d'administration affiche une liste d'images dans un tableau, permet à l'administrateur d'ajouter de nouvelles images et de mettre à jour ou supprimer des images existantes. La confirmation est demandée avant la suppression. -->


