<h1 class="mb-3 text-center">Admin Delete</h1>

<form method="POST">
<div class="mb-3">
  <label for="title" class="form-label">Title</label>
  <input type="text" class="form-control" name="title" id="title" placeholder="Title">
</div>
<div class="mb-3">
  <label for="description" class="form-label">Description</label>
  <input type="text" class="form-control" name="description" id="description" placeholder="Description">
</div>
<div class="mb-3">
  <label for="src" class="form-label">Source</label>
  <input type="text" class="form-control" name="src" id="src" placeholder="URL Source">
</div>
<div class="mb-3">
  <label for="author" class="form-label">Author</label>
  <input type="text" class="form-control" name="author" id="author" placeholder="Author">
</div>
<div class="mb-3">
  <input type="submit" class="btn btn-primary" name="Delete" value="Delete">
</div>
</form>


































<!-- Explications de ce code 


  Ce code génère un formulaire pour supprimer une image de la liste. Cependant, il semble y avoir une confusion, car les champs du formulaire sont destinés à être remplis pour effectuer une suppression. Voici une explication de chaque partie du code :

  1. **`<h1 class="mb-3 text-center">Admin Delete</h1>`** :
    - Cette ligne affiche un titre "Admin Delete" centré horizontalement en haut de la page, indiquant que cette page est destinée à la suppression d'une image.

  2. **`<form method="POST">`** :
    - Cette balise `<form>` démarre un formulaire HTML qui permettra à l'utilisateur de supprimer une image. La méthode "POST" indique que les données du formulaire seront envoyées au serveur lors de la soumission du formulaire.

  3. **Champs de formulaire** :
    - Il y a plusieurs champs de formulaire, chacun étiqueté avec un libellé (balise `<label>`) et un champ de saisie (balise `<input>`). Cependant, ces champs de formulaire sont destinés à être remplis pour effectuer la suppression, ce qui est inhabituel. Voici une explication pour chaque champ :
      - **`<label for="title" class="form-label">Title</label>`** : Un champ de saisie pour le titre de l'image à supprimer.
      - **`<label for="description" class="form-label">Description</label>`** : Un champ de saisie pour la description de l'image à supprimer.
      - **`<label for="src" class="form-label">Source</label>`** : Un champ de saisie pour l'URL source de l'image à supprimer.
      - **`<label for="author" class="form-label">Author</label>`** : Un champ de saisie pour le nom de l'auteur de l'image à supprimer.

  4. **Bouton "Delete"** :
    - **`<input type="submit" class="btn btn-primary" name="Delete" value="Delete">`** : Ce bouton "Submit" permet à l'utilisateur de soumettre le formulaire pour supprimer l'image. Il est stylisé comme un bouton Bootstrap avec la classe "btn btn-primary". Lorsque l'utilisateur clique sur ce bouton, les données du formulaire seront envoyées au serveur pour effectuer la suppression de l'image.

  En résumé, ce code génère un formulaire de suppression d'image. Cependant, il est inhabituel d'avoir des champs de formulaire à remplir pour effectuer une suppression, car normalement, la suppression d'une image serait déclenchée par un simple bouton ou un lien sans nécessiter de saisir des données. -->