






<h1 class="text-center">Gallery</h1>

<br>


<section  class="row">
<?php foreach ($pictures as $pic) : ?>

<article class="col-12 col-sm-6 col-md-4">
  <div  class="card mb-3" data-aos="fade-up">
  <a href="<?= "?page=detail&id=".$pic['id'] ?>">
    <img src="<?= $pic['src'] ?>" class="card-img-top" class="img-fluid" width="550px" height="550px" alt="<?= $pic['title'] ?>">
  </a>
    <div class="card-body" data-aos="fade-down">
    <a class="text-dark text-decoration-none" href="<?= "?page=detail&id=".$pic['id'] ?>">
      <h4 class="card-title"><?= $pic['title'] ?></h4>
    </a>
      <p class="card-text"><?= $pic['description'] ?></p>
      <p class="card-text"><?= $pic['author'] ?></p>
      <a href="<?= "?page=detail&id=".$pic['id'] ?>" class="btn btn-secondary">En savoir davantage</a>
    </div>
  </div>
</article>

<?php endforeach ?>
</section>
 
<script>





</script>




    </div>





    </article>
    




 <!--$pic['id'] = va récupérer id de la table, $pic['title'] récupère le titre de de la table...-->
 <!--boucle foreach pour récupérer tous les éléments de $pictures



Explications de ce code

   Ce code HTML génère une page "Gallery" qui affiche une liste d'images avec leurs titres, descriptions et auteurs. Voici une explication de chaque partie du code :

   1. **`<h1 class="text-center">Gallery</h1>`** :
      - Cette ligne affiche un titre "Gallery" centré horizontalement dans la page.

   2. **`<br>`** :
      - Cette balise `<br>` insère un saut de ligne (ligne vide) pour ajouter de l'espace entre le titre et la galerie d'images.

   3. **`<section class="row">`** :
      - Il s'agit d'une balise `<section>` avec la classe CSS "row". Cette balise est utilisée pour regrouper les images de la galerie dans une grille.

   4. **`< ?php foreach ($pictures as $pic) : ?>`** :
      - C'est le début d'une boucle `foreach` en PHP. Elle parcourt le tableau `$pictures`, qui contient les images de la galerie, et traite chaque image sous le nom `$pic`.

   5. **`<article class="col-12 col-sm-6 col-md-4">`** :
      - C'est une balise `<article>` avec des classes CSS pour définir son comportement dans une grille (à l'aide de Bootstrap). Elle est utilisée pour encapsuler chaque image dans la liste de résultats. Les classes "col-12 col-sm-6 col-md-4" définissent le nombre de colonnes que cet article doit occuper dans une grille réactive en fonction de la taille de l'écran.

   6. **`<div class="card mb-3 data-aos="fade-up" data-aos-anchor-placement="top-bottom">`** :
      - Cette balise `<div>` contient la carte d'une image de la galerie. La classe "card" est souvent utilisée pour styliser les éléments de cartes Bootstrap. Les attributs `data-aos` sont utilisés pour ajouter des effets d'animation lors du défilement de la page.

   7. **`<a href="< ?= "?page=detail&id=".$pic['id'] ?>">`** :
      - C'est un lien hypertexte qui enveloppe l'image de la carte et dirige vers une page de détails de l'image. L'attribut "href" est généré dynamiquement en fonction de l'ID de l'image.

   8. **`<img src="< ?= $pic['src'] ?>" class="card-img-top" class="img-fluid" width="550px" height="550px" alt="< ?= $pic['title'] ?>">`** :
      - Cette ligne affiche l'image de la carte. L'attribut "src" contient l'URL de l'image, l'attribut "alt" contient la description de l'image. Les classes "card-img-top" et "img-fluid" sont utilisées pour le formatage de l'image, et les attributs "width" et "height" définissent les dimensions de l'image.

   9. **`<div class="card-body">`** :
      - Cette balise `<div>` contient le corps de la carte, qui affiche le titre, la description, l'auteur et un bouton pour en savoir davantage sur l'image.

   10. **`<a class="text-dark text-decoration-none" href="< ?= "?page=detail&id=".$pic['id'] ?>">`** :
      - C'est un autre lien hypertexte qui enveloppe le titre de la carte et dirige vers la page de détails de l'image. La classe "text-dark text-decoration-none" est utilisée pour styliser le lien.

   11. **`<h4 class="card-title">< ?= $pic['title'] ?></h4>`** :
      - Cette ligne affiche le titre de l'image dans la carte.

   12. **`<p class="card-text">< ?= $pic['description'] ?></p>`** :
      - Cette ligne affiche la description de l'image dans la carte.

   13. **`<p class="card-text">< ?= $pic['author'] ?></p>`** :
      - Cette ligne affiche l'auteur de l'image dans la carte.

   14. **`<a href="< ?= "?page=detail&id=".$pic['id'] ?>" class="btn btn-secondary">En savoir davantage</a>`** :
      - C'est un bouton qui dirige vers la page de détails de l'image. L'attribut "href" est généré dynamiquement en fonction de l'ID de l'image. La classe "btn btn-secondary" est utilisée pour le formatage du bouton.

   15. **`< ?php endforeach ?>`** :
      - C'est la fin de la boucle `foreach`. Elle marque la fin du traitement des images de la galerie.

   En résumé, ce code génère une page de galerie d'images avec une liste d'images affichées dans des cartes. Chaque carte contient une image, un titre, une description, un auteur et un bouton pour en savoir davantage. L'utilisateur peut cliquer sur chaque image pour accéder à une page de détails de l'image.