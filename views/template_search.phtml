<h1 class="text-center">Search</h1>
<h2 class="text-center">Found <?= count($pictures) ?> result(s)</h2>
<section class="row">
  <?php foreach ($pictures as $pic) : ?>

    <article class="col-12 col-sm-6 col-md-4">
      <div class="card mb-3">
        <img src="<?= $pic['src'] ?>" class="card-img-top" alt="<?= $pic['title'] ?>">
        <div class="card-body">
          <h4 class="card-title"><?= $pic['title'] ?></h4>
          <p class="card-text"><?= $pic['description'] ?></p>
          <p class="card-text"><?= $pic['updated_at'] ?></p>
          <p class="card-text"><?= $pic['author'] ?></p>
          <a href="<?= "?page=detail&id=".$pic['id'] ?>" class="btn btn-secondary">Read more</a>
        </div>
      </div>
    </article>

  <?php endforeach ?>
</section>











































<!-- 

Explications de ce code 

   Ce code HTML est destiné à afficher les résultats de la recherche d'images (ou photos) dans une page web. Il utilise à la fois HTML et PHP pour afficher les informations liées aux images. Voici une explication de chaque partie du code :

   1. **`<h1 class="text-center">Search</h1>`** :
      - Il s'agit d'un en-tête de niveau 1 (balise `<h1>`) avec une classe CSS "text-center" qui centre le texte horizontalement. Le texte "Search" est affiché au centre de la page et indique probablement le titre de la page de résultats de recherche.

   2. **`<h2 class="text-center">Found < ?= count($pictures) ?> result(s)</h2>`** :
      - C'est un en-tête de niveau 2 (balise `<h2>`) avec la classe "text-center". Il affiche le nombre de résultats trouvés dans la recherche en utilisant PHP. La variable `$pictures` semble contenir les résultats de la recherche, et `count($pictures)` donne le nombre de résultats.

   3. **`<section class="row">`** :
      - Il s'agit d'une balise `<section>` avec la classe CSS "row". Cette balise est utilisée pour regrouper les résultats de la recherche. La classe "row" est probablement destinée à utiliser un système de grille pour afficher les résultats dans des colonnes.

   4. **`< ?php foreach ($pictures as $pic) : ?>`** :
      - C'est le début d'une boucle `foreach` en PHP. Elle parcourt le tableau `$pictures`, qui contient les résultats de la recherche, et pour chaque élément, il le traite sous le nom `$pic`.

   5. **`<article class="col-12 col-sm-6 col-md-4">`** :
      - C'est une balise `<article>` avec des classes CSS pour définir son comportement dans une grille (à l'aide de Bootstrap). Elle est utilisée pour encapsuler chaque image dans la liste de résultats. La classe "col-12 col-sm-6 col-md-4" définit le nombre de colonnes que cet article doit occuper dans une grille réactive en fonction de la taille de l'écran.

   6. **`<div class="card mb-3">`** :
      - Cette balise `<div>` a la classe "card" pour formater l'image et ses informations comme une carte. La classe "mb-3" est probablement utilisée pour définir une marge inférieure.

   7. **`<img src="< ?= $pic['src'] ?>" class="card-img-top" alt="< ?= $pic['title'] ?>">`** :
      - C'est une balise `<img>` qui affiche l'image. L'attribut "src" est défini en utilisant la valeur de `$pic['src']`, ce qui charge l'URL de l'image. L'attribut "alt" contient la description de l'image.

   8. **`<div class="card-body">`** :
      - Cette balise `<div>` contient le corps de la carte, qui affiche le titre de l'image, la description, la date de mise à jour et l'auteur.

   9. **`<h4 class="card-title">< ?= $pic['title'] ?></h4>`** :
      - C'est un titre de la carte qui affiche le titre de l'image en utilisant la valeur de `$pic['title']`.

   10. **`<p class="card-text">< ?= $pic['description'] ?></p>`** :
      - C'est un paragraphe de la carte qui affiche la description de l'image en utilisant la valeur de `$pic['description']`.

   11. **`<p class="card-text">< ?= $pic['updated_at'] ?></p>`** :
      - Un autre paragraphe de la carte affichant la date de mise à jour de l'image en utilisant la valeur de `$pic['updated_at']`.

   12. **`<p class="card-text">< ?= $pic['author'] ?></p>`** :
      - Un autre paragraphe de la carte qui affiche l'auteur de l'image en utilisant la valeur de `$pic['author']`.

   13. **`<a href="< ?= "?page=detail&id=".$pic['id'] ?>" class="btn btn-secondary">Read more</a>`** :
      - Il s'agit d'un lien "Lire la suite" qui redirige vers une page de détail de l'image. L'attribut "href" est généré dynamiquement en fonction de l'ID de l'image, en utilisant la valeur de `$pic['id']`.

   14. **`< ?php endforeach ?>`** :
      - C'est la fin de la boucle `foreach` en PHP. Il marque la fin du traitement des résultats de la recherche.

   Dans l'ensemble, ce code génère une liste de résultats de recherche d'images avec leurs titres, descriptions, dates de mise à jour, auteurs, et un lien vers une page de détail pour chaque image. La mise en page est gérée à l'aide de classes CSS et un système de grille, probablement basé sur Bootstrap.  -->